# BookAlchemy

A simple Flask application to manage a digital library.
You can add authors and books, view the collection, and delete entries.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
